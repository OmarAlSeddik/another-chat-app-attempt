import Button from "../Shared/Button";

const Auth = () => {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <Button color="blue" />
    </div>
  );
};

export default Auth;
