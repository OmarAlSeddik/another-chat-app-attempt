const RoomFooter = () => {
  return (
    <div className="relative h-12 shrink-0 border-t border-primary1 bg-primary2">
      <input
        className="absolute bottom-[50%] left-4 right-4 flex h-12 items-center rounded-lg bg-primary5 px-4 outline-none"
        placeholder="Input text here..."
      />
    </div>
  );
};

export default RoomFooter;
